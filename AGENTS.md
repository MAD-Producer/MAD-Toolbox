# MAD-Toolbox开发指南

## 项目信息

- 跨平台（Windows & Mac）多媒体处理工具箱，检查 MAD-Toolbox
- 使用 [Tauri](https://tauri.app/) 框架开发，前端 React + [Mantine v9](https://mantine.dev/) + Zustand，后端 Rust
- 支持bilibili视频下载，YT视频下载，音乐全网下载，视频转码和统一任务调度

## 1. 先思考再编码

**"不要默认假设。不要隐藏困惑。呈现权衡。"**

**每个人的想法都受限于认知，注意提醒用户的Unknown unknown。**

实现之前：

- 明确陈述假设；如果不确定或不明白，就提问。不要擅自为用户做决定。
- 当存在多种理解时，逐一列出而非默默选择。
- 如果存在更简单的方案，直接说明并在必要时提出异议。

## 2. 简洁优先

**"用最少的代码解决问题。不做臆测性编码。"**

- 不实现超出需求的特性。
- 不为仅使用一次的代码做抽象。
- 不添加未经要求的"灵活性"或"可配置性"。
- 不处理在当前系统边界内不会发生的错误场景。

## 3. 精准改动

**"只改必须改的。只清理自己制造的遗留。"**

编辑现有代码时：

- 不要"改善"相邻的代码、注释或格式。
- 不要重构没有问题的代码。
- **与现有风格保持一致。**
- 如果发现无关的废弃代码，提出来而不是直接删除。
- 前端UI设计中不要自主加上MagicNumber，如果有，比如向用户报告

当你的改动产生了孤立的代码时：

- 移除因你的改动而变得未使用的 import/变量/函数。
- 不要移除之前就存在的废弃代码，除非被明确要求。

检验标准："每一行改动都应该能追溯到用户的请求。"

## 4. 浏览器调试

在内置浏览器调试时，遇到以下情况：

- 页面空白
- 页面长时间未加载完成
- 页面行为仍按照旧代码运行

需要：结束该进程，并重新运行`vite`

## 5. 验证尺度

- 简单任务（小幅样式/文案/结构改动）完成后不必截图自查，用户会直接给出反馈
- 截图等浏览器视觉验证只用于复杂交互或视觉存疑的场景

## 6. 前端代码开发

- 硬规则：只有 `*/api.ts` 文件允许 `import { invoke } from "@tauri-apps/api/core"`，组件、hook、store 一律不直接碰 `invoke`。

## 7. 注释纪律

**"注释是例外，不是常态。"**

- 文件中间不加注释，除非它对整个项目的构建起到关键作用：真相源指向、跨文件/跨端契约、无法从代码本身看出的约束（ workaround、魔法数的标定依据）。
- 解释"代码在做什么"的注释一律不加，代码应自解释。
- 拿不准加还是不加时，不加。
- 文件头部或导出 API 上的契约性说明可以保留，但也应尽量短。
